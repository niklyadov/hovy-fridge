<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="40dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/fridge_add_dialog_fridge_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/enter_the_fridge_name"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <EditText
        android:id="@+id/fridge_add_dialog_fridge_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/fridge_add_dialog_fridge_text"
        app:layout_constraintStart_toStartOf="@+id/fridge_add_dialog_fridge_text"
        android:autofillHints="Unknown"
        android:inputType="text">
        <requestFocus />

    </EditText>

</androidx.constraintlayout.widget.ConstraintLayout>